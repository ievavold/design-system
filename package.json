{
  "name": "design-system",
  "version": "0.1.0",
  "description": "a design system",
  "scripts": {
    "start": "hugo server",
    "clean": "rimraf public",
    "deploy": "npm run clean && hugo -b /design-system && gh-pages -d public"
  },
  "repository": "github:ievavold/design-system",
  "author": "Isaac Evavold <ievavold@wisc.edu>",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "normalize.css": "^8.0.1",
    "rimraf": "^2.6.3",
    "sass": "^1.18.0"
  }
}
